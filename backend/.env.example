# Local development database connection (PostgreSQL)
# Update the host/port/credentials if you change the docker-compose config.
DATABASE_URL="postgresql://quizbase:quizbase@localhost:5433/quizbase?schema=public"

# CORS configuration for the API (frontend dev server default)
CORS_ORIGIN="http://localhost:5173"
